```sql
SELECT PT_NAME, PT_NO, GEND_CD, AGE, IFNULL(TLNO, 'NONE') AS TLNO
FROM PATIENT
WHERE AGE <=12 AND GEND_CD = 'W'
ORDER BY AGE desc, PT_NAME
```

## KEY 

```
IFNULL(TLNO, 'NONE') AS TLNO  -> TLNO가 없으면 NONE을 출력하라
```